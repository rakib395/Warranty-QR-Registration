<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_ms_warranty_registration_tree" model="ir.ui.view">
        <field name="name">ms.warranty.registration.list</field>
        <field name="model">ms.warranty.registration</field>
        <field name="arch" type="xml">
            <list string="Warranty Registrations" decoration-info="state == 'pending'" decoration-success="state == 'approved'">
                <field name="name"/>
                <field name="customer_name"/>
                <field name="product_id"/>
                <field name="serial_no"/>
                <field name="expiry_date"/>
                <field name="state" widget="badge" decoration-info="state == 'pending'" decoration-success="state == 'approved'"/>
            </list>
        </field>
    </record>

    <record id="view_ms_warranty_registration_form" model="ir.ui.view">
        <field name="name">ms.warranty.registration.form</field>
        <field name="model">ms.warranty.registration</field>
        <field name="arch" type="xml">
            <form string="Warranty Registration">
                <header>
                    <button name="action_submit" string="Pending Approval" type="object" 
                            class="oe_highlight" invisible="state != 'draft'"/>

                    <button name="action_approve" string="Approve" type="object" 
                            class="oe_highlight" invisible="state != 'pending'"/>
                    
                    <button name="%(action_warranty_extend_wizard)d" string="Extend Warranty" type="action" 
                            class="oe_highlight" invisible="state != 'approved'"/>
                    
                    <button name="%(action_warranty_transfer_wizard)d" string="Transfer Ownership" type="action" 
                            class="oe_highlight" invisible="state != 'approved'"/>
                    
                    <button name="action_reject" string="Reject" type="object" 
                            invisible="state != 'pending'"/>

                    <button name="action_draft" string="Set to Draft" type="object" 
                            invisible="state not in ['approved', 'rejected', 'expired']"/>

                    <button name="action_expire" string="Set Expired" type="object" 
                            invisible="state != 'approved'"/>

                    <field name="state" widget="statusbar" statusbar_visible="draft,pending,approved,expired"/>
                </header>
                
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>

                    <group>
                        <group string="Customer Information">
                            <field name="customer_name"/>
                            <field name="customer_phone"/>
                            <field name="customer_email"/>
                        </group>
                        <group string="Product Information">
                            <field name="product_id" readonly="1" force_save="1"/>
                            <field name="serial_no" readonly="1" force_save="1"/>
                            <field name="policy_id"/>
                        </group>
                    </group>

                    <group>
                        <group string="Purchase Details">
                            <field name="purchase_date"/>
                            <field name="dealer_id"/>
                            <field name="invoice_name" invisible="1"/>
                            <field name="invoice_proof" filename="invoice_name"/>
                        </group>
                        <group string="Warranty Dates">
                            <field name="registration_date"/>
                            <field name="expiry_date" readonly="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Ownership Transfer History">
                            <group string="Previous Owner Information">
                                <field name="previous_owner_name" readonly="1"/>
                                <field name="previous_owner_phone" readonly="1"/>
                                <field name="previous_owner_email" readonly="1"/>
                            </group>

                            <group string="Latest Transfer Details">
                                <field name="last_transfer_date" readonly="1"/>
                                <field name="last_transfer_note" readonly="1"/>
                                <field name="ownership_transfer_count" readonly="1"/>
                            </group>

                        </page>
                        <page string="Extension History" name="warranty_action_logs">
                            <field name="event_log_ids" readonly="1">
                                <list string="Audit logs">
                                    <field name="create_date" string="Action Date"/>
                                    <field name="user_id" string="Authorized User"/>
                                    <field name="event_type" string="Event"/>
                                    <field name="old_expiry_date" string="Previous Expiry"/>
                                    <field name="new_expiry_date" string="Extended Expiry"/>
                                    <field name="reason" string="Justification/Reason"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_ms_warranty_registration" model="ir.actions.act_window">
        <field name="name">Warranty Registrations</field>
        <field name="res_model">ms.warranty.registration</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>